<?xml version="1.0" encoding="utf-8" ?>
<component name="TheClassicalStation" extends="Scene">
    <children>
        <Poster
                id="wcpePoster"
                uri="pkg:/images/theclassicalstation_svg_1920x1080.png"
                loadDisplayMode="scaleToFit"
        />
    </children>
<!-- BrightScript Portion -->
<script type="text/brightscript" >
<![CDATA[
'**
'** Example: Edit a Label size and color with BrightScript
'**

function init()
    m.top.setFocus(true)

    ' https://developer.roku.com/docs/references/scenegraph/control-nodes/contentnode.md
    ' https://developer.roku.com/docs/references/scenegraph/media-playback-nodes/audio.md
    audio = createObject("roSGNode", "Audio")
    audiocontent = createObject("RoSGNode", "ContentNode")
    audiocontent.url = "http://playerservices.streamtheworld.com/api/livestream-redirect/WCPE_FM_ADP.aac"
    audio.content = audiocontent
    m.top.appendChild(audio)
    audio.control = "play"
    m.top.setFocus(true)

    m.audio = audio
end function

function onKeyEvent(key as String, press as Boolean) as Boolean
    handled = false
    if press then
        if (key = "play") then
            if (m.audio.state = "playing") then
                m.audio.control = "stop"
            else
                m.audio.control = "play"
            end if
            handled = true
        end if
    end if
    return handled
end function
]]>
</script>
<!-- End of BrightScript Portion -->
</component>
